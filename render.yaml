services:
  - type: web
    name: book-recommender
    env: python
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
   startCommand: "python check_python.py && streamlit run app.py --server.port 8080"
    envVars:
      - key: PORT
        value: 8080
